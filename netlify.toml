[build]
  base = "react-app"
  publish = "dist"
  command = "npm run build"

[dev]
  command = "npm run dev"
  port = 3000
  targetPort = 3000
  framework = "vite"

[build.environment]
  NODE_VERSION = "18"
